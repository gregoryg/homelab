apiVersion: v1
kind: Service
metadata:
  labels:
    app: tigergraph
    {{- include "tiger.selectorLabels" . | nindent 4 }}
  name: tg-external-service
spec:
  type: LoadBalancer
  selector:
    app: tigergraph
  ports:
  - port: 9000
    name: rest
    targetPort: 9000
  - port: 14240
    name: graphstudio
    targetPort: 14240
